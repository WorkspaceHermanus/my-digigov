# Fonts

The site currently uses a modern system font stack defined in `css/variables.css`.

Add licensed local font files here if a brand font is supplied later, then register them with `@font-face` in `css/base.css` or a dedicated font stylesheet.
